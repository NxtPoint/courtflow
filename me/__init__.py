# me/ — the client self-service ("My Account") lane. /api/me/* endpoints scoped to the
# CALLER's own principal (user_id + club_id from auth.resolve_principal). Profile demographics
# + dependents/children CRUD. See docs/specs/client-self-service-spec.md.
