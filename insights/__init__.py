# insights/ — Phase 2 "P1 insight read-layer" (docs/specs/ADMIN-PHASE2.md).
# Guarded, club_id-scoped, read-only aggregations over the EXISTING diary/billing/core data —
# NO new tables. The single place metrics are computed (no duplicated aggregation logic).
